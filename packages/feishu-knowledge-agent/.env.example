PORT=8788
PUBLIC_BASE_URL=https://your-domain.example.com

# Feishu custom app credentials.
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx
FEISHU_VERIFICATION_TOKEN=xxx
# Optional. If set, request signatures are verified.
FEISHU_ENCRYPT_KEY=
# Optional delayed group reminder after a successful archive.
# Put the group chat_id here. Leave empty to disable.
FEISHU_ARCHIVE_REMINDER_CHAT_ID=
FEISHU_ARCHIVE_REMINDER_DELAY_MINUTES=5
FEISHU_ARCHIVE_REMINDER_CANCEL_WINDOW_MINUTES=1
FEISHU_ARCHIVE_REMINDER_SKIP_SOURCE_CHAT=true

# Optional Feishu Bitable sink. The bot still works with the local JSON store if these are empty.
FEISHU_BITABLE_APP_TOKEN=
FEISHU_BITABLE_TABLE_ID=

# Optional browsable Feishu doc. Create it once with `npm run doc:create`, then paste the id here.
# The doc is rebuilt from .knowledge/records.json after every archive.
FEISHU_DOC_ID=
# Or paste a full Feishu docx/wiki URL here. Mark will use this link in replies.
FEISHU_DOC_URL=
# Folder the doc is created in. Leave empty for the app's root folder.
FEISHU_DOC_FOLDER_TOKEN=
# Optional separate document for saved decisions and their evidence.
FEISHU_DECISION_DOC_ID=
FEISHU_DECISION_DOC_URL=
FEISHU_DECISION_DOC_FOLDER_TOKEN=
FEISHU_FIELD_TITLE=Title
FEISHU_FIELD_SUMMARY=Summary
FEISHU_FIELD_CATEGORY=Category
FEISHU_FIELD_TAGS=Tags
FEISHU_FIELD_SOURCE_URL=Source URL
FEISHU_FIELD_SOURCE_TYPE=Source Type
FEISHU_FIELD_USE_CASES=Use Cases
FEISHU_FIELD_SHARER=Sharer
FEISHU_FIELD_CREATED_AT=Created At

# Pi runner. Set this to the pi binary you want this service to call.
PI_AGENT_BINARY=pi
PI_AGENT_TIMEOUT_MS=120000

# LLM analysis. OpenAI-compatible API; used before the Pi runner when configured.
MARK_LLM_BASE_URL=https://thebestai.net/v1
MARK_LLM_API_KEY=
MARK_LLM_MODEL=qwen-plus
MARK_LLM_TIMEOUT_MS=120000
# Token accounting. Prices are per million tokens and cannot be read from the API,
# because the endpoint is a reseller gateway. Leave empty to report tokens without money.
# DeepSeek list prices for reference: input 2, cached input 0.2, output 8 (CNY).
MARK_LLM_PRICE_INPUT_PER_1M=
MARK_LLM_PRICE_CACHED_INPUT_PER_1M=
MARK_LLM_PRICE_OUTPUT_PER_1M=
MARK_LLM_PRICE_CURRENCY=元

# General decision agent. Monid is recommended when MONID_API_KEY is configured.
# Brave Search and custom SearXNG-compatible JSON endpoints are also supported.
MARK_DECISION_MAX_STEPS=8
MARK_DECISION_MAX_SOURCES=5
MARK_DECISION_INTERNAL_LIMIT=5
MARK_WEB_SEARCH_ENABLED=true
MARK_WEB_SEARCH_PROVIDER=monid
MARK_WEB_SEARCH_API_KEY=
MARK_WEB_SEARCH_URL=https://api.search.brave.com/res/v1/web/search?q={query}
MARK_WEB_SEARCH_TIMEOUT_MS=15000

# Monid search. Keep the key server-side. Used for finding public pages, not link extraction.
MONID_API_KEY=
MONID_BASE_URL=https://api.monid.ai
MONID_SEARCH_PROVIDER=context.dev
MONID_SEARCH_ENDPOINT=/web/search

# X/Twitter. Optional fallback for post body, author, media, and metrics extraction.
X_BEARER_TOKEN=

# Bilibili subtitle extraction. Calls the Bilibili web APIs directly; no Python needed.
# Credentials come from these variables, or from BILIBILI_CONFIG_FILE when they are empty.
BILIBILI_SESSDATA=
BILIBILI_BILI_JCT=
BILIBILI_BUVID3=
BILIBILI_CONFIG_FILE=
BILIBILI_TIMEOUT_MS=20000
# Optional custom override. Command must print JSON to stdout and may use {url}.
BILIBILI_TRANSCRIPT_COMMAND=

# YouTube subtitle extraction. No API key is required for public captions.
YOUTUBE_CAPTION_LANGUAGES=zh-Hans,zh-CN,zh,en
YOUTUBE_TIMEOUT_MS=20000
# YouTube answers datacenter IPs with "sign in to confirm you're not a bot" and an empty
# caption list, so reading captions from a server needs a signed-in cookie.
# Export youtube.com cookies from a browser (Netscape cookies.txt) to this path.
# Both this file and YOUTUBE_COOKIE are Git-ignored; use a throwaway account, not your main one.
YOUTUBE_COOKIE_FILE=
# Alternative to the file: a raw "name=value; name2=value2" header. Wins over the file.
YOUTUBE_COOKIE=

# Tencent Cloud server monitoring. Without these, Mark still reports local server status.
TENCENTCLOUD_SECRET_ID=
TENCENTCLOUD_SECRET_KEY=
TENCENTCLOUD_SECURITY_TOKEN=
TENCENTCLOUD_REGION=ap-guangzhou
TENCENTCLOUD_CVM_INSTANCE_ID=
TENCENTCLOUD_CVM_INSTANCE_ALIAS=
# For multiple servers, prefer this format:
# TENCENTCLOUD_CVM_INSTANCES=ap-guangzhou:ins-xxxx:Mark主机,ap-shanghai:ins-yyyy:数据库
TENCENTCLOUD_CVM_INSTANCES=
TENCENTCLOUD_MONITOR_NAMESPACE=QCE/CVM
TENCENTCLOUD_MONITOR_DIMENSION_NAME=InstanceId
TENCENTCLOUD_MONITOR_METRICS=CpuUsage,CpuLoadavg,MemUsage,CvmDiskUsage,WanIntraffic,WanOuttraffic,TcpCurrEstab
TENCENTCLOUD_MONITOR_PERIOD=60
TENCENTCLOUD_MONITOR_WINDOW_MINUTES=10
TENCENTCLOUD_TIMEOUT_MS=10000

# Local fallback store.
KNOWLEDGE_DATA_DIR=.knowledge
